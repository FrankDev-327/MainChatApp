import { CheckTokenGuard } from './check.token.guard';

describe('CheckTokenGuard', () => {
  let guard: CheckTokenGuard;

  beforeEach(() => {
    //guard = new CheckTokenGuard();
  });


  it('should be defined', () => {
    expect(guard).toBeDefined();
  });
});
